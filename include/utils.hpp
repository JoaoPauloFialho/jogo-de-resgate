#ifndef UTILS_HPP
#define UTILS_HPP
#include <string>
#include <vector>
using namespace std;

namespace util{
    int calcularPorcentagem(float valor, float total);
    int achaItemEmVetor(vector<string> &vector, string item) ;
}

#endif